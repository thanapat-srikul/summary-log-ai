# Security Policy

Please do not disclose vulnerabilities in public issues. Contact the project maintainer privately and include the affected version, reproduction steps and impact.

Supported version: the latest `0.1.x` release.

Operators should rotate a Source API key immediately if it may have been exposed, keep `APP_SECRET` and database backups outside source control, enable HTTPS, restrict server administration ports and update container images regularly.
